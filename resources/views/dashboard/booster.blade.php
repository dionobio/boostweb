@extends('layouts.boostxvn')
@section('content')
<div class="dashboard-layout"><div class="sidebar"><div class="sidebar-section"><div class="sidebar-label">Booster</div><a class="sidebar-item active" href="{{ route('dashboard.booster') }}">🎮 Đơn đang làm</a><a class="sidebar-item" href="{{ route('marketplace.index') }}">🛒 Marketplace</a><a class="sidebar-item" href="{{ route('wallet.index') }}">💰 Thu nhập</a></div></div><div class="dash-content"><div class="dash-header"><h1>Dashboard booster</h1><p>Quản lý đơn nhận và thu nhập.</p></div><div class="stats-row"><div class="stat-card"><div class="sc-label">Đơn nhận</div><div class="sc-value">{{ $orders->count() }}</div></div><div class="stat-card"><div class="sc-label">Thu nhập</div><div class="sc-value">{{ number_format($income,0,',','.') }}₫</div></div><div class="stat-card"><div class="sc-label">Đang làm</div><div class="sc-value">{{ $orders->whereIn('status',['accepted','submitted'])->count() }}</div></div><div class="stat-card"><div class="sc-label">Hoàn thành</div><div class="sc-value">{{ $orders->where('status','completed')->count() }}</div></div></div><div class="dash-table"><div class="dash-table-header"><h3>Đơn đã nhận</h3></div><table><thead><tr><th>Đơn</th><th>Khách</th><th>Trạng thái</th><th>Nhận</th><th></th></tr></thead><tbody>@foreach($orders as $order)<tr><td>{{ $order->title }}</td><td>{{ $order->customer->name }}</td><td>{{ $order->status }}</td><td>{{ number_format($order->booster_receive,0,',','.') }}₫</td><td><a class="btn btn-sm btn-primary" href="{{ route('orders.room',$order) }}">Room</a></td></tr>@endforeach</tbody></table></div></div></div>
@endsection
