@extends('layouts.boostxvn')
@section('content')
<div class="dashboard-layout"><div class="sidebar"><div class="sidebar-section"><div class="sidebar-label">Khách hàng</div><a class="sidebar-item active" href="{{ route('dashboard.customer') }}">📦 Đơn đã đăng</a><a class="sidebar-item" href="{{ route('wallet.index') }}">💳 Ví tiền</a><a class="sidebar-item" href="{{ route('orders.create') }}">➕ Tạo đơn</a></div></div><div class="dash-content"><div class="dash-header"><h1>Dashboard khách hàng</h1><p>Theo dõi đơn hàng, ký quỹ và trạng thái xử lý.</p></div><div class="stats-row"><div class="stat-card"><div class="sc-label">Tổng đơn</div><div class="sc-value">{{ $orders->count() }}</div></div><div class="stat-card"><div class="sc-label">Đang mở</div><div class="sc-value">{{ $orders->where('status','open')->count() }}</div></div><div class="stat-card"><div class="sc-label">Đang làm</div><div class="sc-value">{{ $orders->whereIn('status',['accepted','submitted'])->count() }}</div></div><div class="stat-card"><div class="sc-label">Hoàn thành</div><div class="sc-value">{{ $orders->where('status','completed')->count() }}</div></div></div><div class="dash-table"><div class="dash-table-header"><h3>Đơn của tôi</h3></div><table><thead><tr><th>Đơn</th><th>Game</th><th>Trạng thái</th><th>Giá</th><th></th></tr></thead><tbody>@foreach($orders as $order)<tr><td>{{ $order->title }}</td><td>{{ $order->game->name }}</td><td>{{ $order->status }}</td><td>{{ number_format($order->budget,0,',','.') }}₫</td><td><a class="btn btn-sm btn-primary" href="{{ route('orders.show',$order) }}">Xem</a></td></tr>@endforeach</tbody></table></div></div></div>
@endsection
