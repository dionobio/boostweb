@extends('layouts.boostxvn')
@section('content')
<div class="container" style="padding:40px 24px 60px;max-width:760px"><div class="sidebar-card"><h1 style="font-size:30px;margin-bottom:8px">Tạo tranh chấp</h1><p style="color:var(--text2);margin-bottom:24px">Admin sẽ xem bằng chứng trong order room và quyết định hoàn tiền/chuyển tiền/phạt.</p><form method="POST" enctype="multipart/form-data" action="{{ route('disputes.store') }}">@csrf<div class="form-group"><label>Đơn hàng</label><select class="form-select" name="order_id" required>@foreach($orders as $order)<option value="{{ $order->id }}" @selected(request('order_id')==$order->id)>{{ $order->code }} - {{ $order->title }}</option>@endforeach</select></div><div class="form-group"><label>Lý do</label><input class="form-input" name="reason" value="{{ old('reason') }}" required></div><div class="form-group"><label>Mô tả</label><textarea class="form-textarea" name="description" required>{{ old('description') }}</textarea></div><div class="form-group"><label>Ảnh bằng chứng</label><input class="form-input" type="file" name="evidence" accept="image/*"></div><button class="btn btn-red" style="width:100%;padding:13px">Gửi tranh chấp</button></form></div></div>
@endsection
